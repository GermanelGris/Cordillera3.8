# Dependencias: se instalan dentro del contenedor (npm ci), nunca copiar las locales
node_modules

# Artefactos de build: los genera "vite build" dentro del contenedor
dist

# Logs y temporales
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Configuración local / control de versiones
.git
.gitignore
.env
.env.local
.env.*.local
.dockerignore
Dockerfile
